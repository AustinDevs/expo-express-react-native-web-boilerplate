import NativeTachyons from 'react-native-style-tachyons';
import { StyleSheet } from 'react-native';
import colors from './colors';

NativeTachyons.build({ colors }, StyleSheet);

export default StyleSheet;